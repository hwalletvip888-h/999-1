{
  "name": "@hwallet/h1-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "H1 layered platform: integration → orchestration → experience (see README)",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
